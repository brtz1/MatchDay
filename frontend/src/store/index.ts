// Central barrel file for store-related hooks and providers.
// Add new contexts/providers here as the project grows so that
// consumers can simply `import { useSomething } from "@/store"`.

export { TeamProvider, useTeamContext } from "./TeamContext";
